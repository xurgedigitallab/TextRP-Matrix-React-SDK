import * as React from 'react';

declare function TabPanel({ children, render, unmount, ...props }: React.HTMLAttributes<HTMLDivElement> & {
    hidden: boolean;
} & ({
    render?: "idle";
    unmount?: "never" | number;
} | {
    render?: "lazy";
    unmount?: "idle" | "never" | number;
})): JSX.Element;

declare function useTabs<K extends string>(tabs: K[], defaultTab?: K | null | undefined): [K | null | undefined, React.Dispatch<React.SetStateAction<K | null | undefined>>];

export { TabPanel, useTabs };
