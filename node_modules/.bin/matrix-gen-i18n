#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../matrix-web-i18n/scripts/gen-i18n.js" "$@"
  ret=$?
else 
  node  "$basedir/../matrix-web-i18n/scripts/gen-i18n.js" "$@"
  ret=$?
fi
exit $ret
